# Normalize line endings to LF in the repo (Go project + Linux CI).
# Keeps shell scripts runnable on Linux and stops CRLF churn on Windows.
* text=auto eol=lf

# Binary files git must not touch
*.png binary
*.ico binary
*.bin binary
