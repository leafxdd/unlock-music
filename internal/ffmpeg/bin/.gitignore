# Generated by build/ffmpeg/build.sh and embedded only in `-tags um_embed_ffmpeg`
# release builds. Not committed by default (multi-MB binaries); CI or a local
# build produces them before an embedding build. See build/ffmpeg/README.md.
*/ffmpeg*.gz
*/ffprobe*.gz
*/version.txt
